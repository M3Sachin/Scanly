{
  "name": "scanly",
  "version": "1.0.0",
  "private": true,
  "description": "Free, no-signup QR code generator with illustrated frames. Static site, no backend.",
  "license": "MIT",
  "scripts": {
    "css": "tailwindcss -c tailwind.config.js -i src/input.css -o styles.css --minify",
    "css:watch": "tailwindcss -c tailwind.config.js -i src/input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
